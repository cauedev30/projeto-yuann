# Artifact F3-G

## Task Brief

- Request: executar o gate `F3-G` para fechar a Fase 3 do MVP LegalTech, verificando coerencia tecnica entre extracao de contrato assinado, eventos e UI, formalizando QA e atualizando docs e memoria.
- Risk level: `high-risk cross-cutting`
- Affected areas: `backend`, `web`, `setup`, `docs`, `memory`, `external sync`
- Required roles: `implementation-manager`, `tech-lead`, `qa-backend`, `qa-frontend`, `documentation-engineer`
- Architecture review required: `yes`
- Required artifacts: task brief, architecture review, backend QA review, frontend QA review, documentation update note
- Key requirements:
  - confirmar o alinhamento `extraction_metadata -> field_confidence -> API -> MetadataSection`
  - confirmar o alinhamento `signed_contract_snapshot -> build_contract_events -> API -> EventTimeline`
  - executar QA backend e frontend com evidencia fresca, incluindo a correcao do lint do frontend
  - publicar os artifacts faltantes de `F3-B` e `F3-G`
  - atualizar `./.codex-memory/` para liberar a proxima fase
- Blocking concerns known at entry:
  - `next lint` estava deprecated e travava em prompt interativo
  - o checkout original em `main` estava sujo fora de `./.codex-memory/`
  - sincronizacao do Trello depende de credenciais ou acesso externo nao confirmados neste harness
- Notes marked `a confirmar`:
  - sincronizacao do card `F3-G` no Trello
  - identificador exato do primeiro card da Fase 4 no board

## Architecture Review

- Reviewer: `tech-lead`
- Scope reviewed:
  - persistencia do snapshot estruturado do contrato assinado
  - derivacao da agenda canonica de eventos
  - serializacao HTTP do detalhe do contrato
  - mapeamento frontend e apresentacao em `MetadataSection`, `EventTimeline` e `ContractDetailScreen`
- Boundaries checked:
  - `backend/app/tasks/archive.py` escreve `signed_contract_snapshot` e `field_confidence` em `contract_version.extraction_metadata`
  - `backend/app/domain/events.py` deriva `renewal`, `expiration`, `readjustment` e `grace_period_end`
  - `backend/app/api/serializers/contracts.py` le `field_confidence` no top-level de `extraction_metadata` e serializa `events`
  - `backend/app/schemas/contract.py` fixa o contrato HTTP de `field_confidence` e `events`
  - `web/src/entities/contracts/model.ts` converte o payload snake_case para tipos camelCase
  - `web/src/features/contracts/components/metadata-section.tsx` e `web/src/features/contracts/components/event-timeline.tsx` consomem o contrato sem JSON cru
- Result: `pass`
- Constraints or required changes:
  - o gate precisou migrar o frontend para ESLint CLI via `web/eslint.config.mjs` e `npm run lint`, porque `next lint` ficou inadequado para verificacao nao interativa
  - o serializer deve continuar lendo `extraction_metadata[\"field_confidence\"]` enquanto esse for o contrato canonico vigente
- Evidence:
  - `backend/app/tasks/archive.py`
  - `backend/app/domain/events.py`
  - `backend/app/api/serializers/contracts.py`
  - `backend/app/schemas/contract.py`
  - `web/src/entities/contracts/model.ts`
  - `web/src/features/contracts/components/metadata-section.tsx`
  - `web/src/features/contracts/components/event-timeline.tsx`
- Durable decision recorded: `yes`
- Notes marked `a confirmar`:
  - nenhuma

## Backend QA Review

- Status: `pass`
- Evidence:
  - `cd backend && .venv/bin/python -m pytest -v` -> `36 passed`
  - a suite verde cobre os pontos centrais da Fase 3: `tests/api/test_contracts_api.py`, `tests/application/test_contract_upload.py`, `tests/services/test_contract_metadata.py`, `tests/services/test_event_scheduler.py` e `tests/tasks/test_archive_task.py`
  - os testes atuais confirmam lista e detalhe com `events`, `field_confidence`, substituicao da agenda por nova versao assinada e persistencia do snapshot estruturado
- Non-blocking notes:
  - `backend/pyproject.toml` segue declarando `requires-python = \">=3.13\"`, enquanto o runtime local verificado nesta sessao foi `Python 3.12.3`

## Frontend QA Review

- Status: `pass`
- Evidence:
  - `cd web && npm run test` -> `69 passed`
  - `cd web && npx tsc --noEmit` -> `pass`
  - `cd web && npm run lint` -> `pass`
  - cobertura diretamente ligada a Fase 3 confirmada em `contract-detail-screen.test.tsx` (`11` testes), `metadata-section.test.tsx` (`12` testes) e `event-timeline.test.tsx` (`11` testes)
- Non-blocking notes:
  - o dashboard continua sem fonte runtime real; isso permanece fora do escopo deste gate
  - a validacao E2E por navegador nao fez parte deste gate e permanece `a confirmar`

## Documentation Update Note

- Updated docs:
  - `docs/squad/artifacts/2026-03-17-f3-b-events-metadata-ui.md`
  - `docs/squad/artifacts/2026-03-17-f3-g-gate-fase-3.md`
  - `./.codex-memory/current-state.md`
  - `./.codex-memory/session-log.md`
  - `./.codex-memory/decisions.md`
- External sync:
  - Trello `a confirmar` neste harness por falta de credenciais ou fluxo local verificavel
- Notes marked `a confirmar`:
  - checklist `F3-G` 5/5 e move para `Concluido` no Trello
  - nome exato do primeiro card liberado da Fase 4
